# 用户体系 0006 迁移验证

- 日期：2026-08-25
- 验证方式：从当前内部 SQLite 数据库复制独立测试库，在副本上执行 Alembic
  `0004 -> 0005 -> 0006`，未修改原数据库。
- 迁移结果：成功。
- 第一批完整后端回归：`215 passed, 3 skipped`，耗时 21.26 秒。
- 第二批完整后端回归：`218 passed, 3 skipped`，耗时 21.16 秒。
- 第三批完整后端回归：`220 passed, 3 skipped`，耗时 21.79 秒。
- 第四批完整后端回归：`221 passed, 3 skipped`，耗时 21.61 秒。
- 前端反馈补强回归：`222 passed, 3 skipped`，耗时 22.62 秒；用户身份契约专项
  `11 passed`。
- 新增覆盖：用户名 NFKC/casefold 规范化、JWT `token_version` 与平台管理员声明、
  V1 创建用户请求契约、结构化错误格式。
- 第二批新增覆盖：用户分页/检索/状态过滤、用户详情、资料乐观锁更新、电话脱敏、
  用户审计、创建用户精确幂等重放、改密后旧令牌撤销和审计密码防泄露。
- 第三批新增覆盖：`unassigned` 身份允许列表和跨文档/会话/通知/数据库/账单负向矩阵、
  创建用户与改密独立限流、稳定限流错误码、改密失败审计及密码防泄露。
- 第四批新增覆盖：创建用户失败审计、可配置弱密码集合、用户迁移 dry-run、用户名冲突预检、
  用户/任职数量守恒、密码哈希 checksum 守恒及 0006 downgrade 回滚。
- 前端反馈补强：允许无任何任职的平台管理员在多租户模式登录，`/login`、`/me` 明确返回
  `organization=null`、`current_membership=null`，并补齐账号状态和密码状态字段。
- 迁移演练报告：`user-0006-pre.json`、`user-0006-post.json`、
  `user-0006-downgrade.json`；三阶段状态均为 `passed`，回滚 checksum 与迁移前一致。
- 未完成验收：真实 PostgreSQL 并发创建、幂等竞争、事务故障注入和多 worker 初始化。

数据库副本及 WAL/SHM 文件已通过 `.gitignore` 排除，避免提交账号和资料数据。
