# SQL-RPA 用户操作手册

> 版本: 1.0 | 更新日期: 2026-07-06

---

## 目录

1. [产品概述](#1-产品概述)
2. [快速入门](#2-快速入门)
3. [配置指南](#3-配置指南)
4. [功能模块](#4-功能模块)
   - 4.1 [AI 聊天](#41-ai-聊天)
   - 4.2 [文档管理](#42-文档管理)
   - 4.3 [数据库操作](#43-数据库操作)
   - 4.4 [系统设置](#44-系统设置)
   - 4.5 [记忆管理](#45-记忆管理)
5. [Agent 工具参考](#5-agent-工具参考)
6. [安全机制](#6-安全机制)
7. [常见问题](#7-常见问题)

---

## 1. 产品概述

SQL-RPA 是一个智能数据库助手系统，支持用户通过**自然语言对话**与数据库交互。系统集成了 RAG（检索增强生成）知识库、Agent 工具调用、数据库操作审核、长期记忆等能力。

### 核心能力

| 能力 | 说明 |
|---|---|
| 自然语言查库 | 用中文描述需求，Agent 自动生成并执行 SQL |
| 知识库问答 | 上传文档后，Agent 基于文档内容回答问题 |
| 写操作审核 | INSERT/UPDATE/DELETE 需人工审核，支持备份回滚 |
| 网络搜索 | 知识库无答案时自动搜索互联网 |
| 长期记忆 | 自动记住用户偏好、决策和事实信息 |
| 多数据源 | 同时支持 SQLite 和 MySQL 目标数据库 |

### 技术架构

```
浏览器 (React) → FastAPI 后端 → Agent 循环
                                 ├→ LLM (OpenAI 兼容)
                                 ├→ 向量检索 (Qdrant/SQLite)
                                 ├→ 全文检索 (FTS5)
                                 ├→ 目标数据库 (SQLite/MySQL)
                                 └→ 网络搜索 (DuckDuckGo)
```

---

## 2. 快速入门

### 2.1 环境要求

- Python 3.12+
- Node.js 20+
- （可选）Tesseract OCR — 用于扫描版 PDF 的文字识别

### 2.2 启动后端

```bash
cd backend

# 创建虚拟环境
python -m venv .venv
source .venv/bin/activate   # Linux/Mac
.venv\Scripts\activate      # Windows

# 安装依赖
pip install -r requirements.txt

# 配置环境变量（复制并编辑 .env 文件）
cp .env.example .env

# 启动服务
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

后端默认运行在 `http://localhost:8000`。

### 2.3 启动前端

```bash
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

前端默认运行在 `http://localhost:5173`。

### 2.4 首次配置

1. 访问 `http://localhost:5173`，进入**设置**页面
2. 配置 LLM 大模型（Provider、Model、API Key、Base URL）
3. 配置 Embedding 嵌入模型（同上格式）
4. 点击「测试连接」确认配置正确
5. 点击「保存设置」

### 2.5 API Key 认证

系统默认启用 API Key 认证。首次启动时系统自动生成 API Key 并写入 `.env` 文件。前端需在 `.env` 中配置：

```
VITE_API_KEY=<.env 中生成的 API_KEY 值>
```

---

## 3. 配置指南

### 3.1 环境变量 (.env)

所有配置项通过项目根目录的 `.env` 文件管理。

#### LLM 大模型

| 变量 | 默认值 | 说明 |
|---|---|---|
| `LLM_PROVIDER` | `openai` | 供应商名称 |
| `LLM_MODEL` | `gpt-4o` | 模型名称 |
| `LLM_API_KEY` | (空) | API 密钥，保存时自动加密 |
| `LLM_BASE_URL` | `https://api.openai.com/v1` | API 地址 |

#### Embedding 嵌入模型

| 变量 | 默认值 | 说明 |
|---|---|---|
| `EMBEDDING_PROVIDER` | `openai` | 供应商名称 |
| `EMBEDDING_MODEL` | `text-embedding-3-small` | 模型名称 |
| `EMBEDDING_API_KEY` | (空) | API 密钥 |
| `EMBEDDING_BASE_URL` | `https://api.openai.com/v1` | API 地址 |
| `EMBEDDING_DIM` | `1536` | 向量维度 |

#### 目标数据库

| 变量 | 默认值 | 说明 |
|---|---|---|
| `DB_TYPE` | `sqlite` | 数据库类型：`sqlite` 或 `mysql` |
| `DB_SQLITE_PATH` | `./data/rpa.db` | SQLite 文件路径 |
| `DB_HOST` | `localhost` | MySQL 主机地址 |
| `DB_PORT` | `3306` | MySQL 端口 |
| `DB_USER` | (空) | 数据库用户名 |
| `DB_PASSWORD` | (空) | 数据库密码，保存时自动加密 |
| `DB_NAME` | (空) | 数据库名称 |
| `DB_POOL_SIZE` | `5` | MySQL 连接池大小 |

#### Agent 参数

| 变量 | 默认值 | 说明 |
|---|---|---|
| `MAX_LOOP_ITERATIONS` | `10` | 单次对话 Agent 最大推理轮次 |
| `MAX_TOTAL_TIME` | `120` | 单次对话最大执行时间（秒） |
| `CHUNK_SIZE` | `512` | 文档分块大小（字符） |
| `CHUNK_OVERLAP` | `50` | 分块重叠大小（字符） |
| `RETRIEVAL_TOP_K` | `8` | 检索返回结果数量 |

#### 功能开关

| 变量 | 默认值 | 说明 |
|---|---|---|
| `WEB_SEARCH_ENABLED` | `true` | 启用网络搜索 |
| `RERANK_ENABLED` | `true` | 启用 Cross-Encoder 重排序 |
| `DEDUP_ENABLED` | `true` | 启用检索结果去重 |
| `MEMORY_ENABLED` | `true` | 启用长期记忆 |
| `OCR_ENABLED` | `true` | 启用 OCR 文字识别 |
| `MEMORY_MAX_COUNT` | `100` | 最大记忆存储数量 |

#### 向量数据库 (Qdrant)

| 变量 | 默认值 | 说明 |
|---|---|---|
| `QDRANT_HOST` | (空) | Qdrant 服务地址。留空则使用本地 SQLite 模式 |
| `QDRANT_PORT` | `6333` | Qdrant 端口 |
| `QDRANT_PATH` | `./data/qdrant` | Qdrant 本地存储路径 |

#### 安全

| 变量 | 默认值 | 说明 |
|---|---|---|
| `SQL_RPA_SECRET_KEY` | (环境变量) | 用于加密 API Key 的主密钥。**不要写入 .env**，应设为系统环境变量 |
| `API_KEY` | (自动生成) | 前端/外部访问的 Bearer Token |

### 3.2 兼容的 LLM 供应商

系统兼容所有 OpenAI 协议接口，已知可用的供应商包括：

- OpenAI
- DeepSeek
- 智谱 (Zhipu / GLM)
- 月之暗面 (Moonshot / Kimi)
- 通义千问 (Qwen / DashScope)
- Groq
- Ollama (本地部署)
- 任何兼容 `/v1/chat/completions` 和 `/v1/embeddings` 的接口

---

## 4. 功能模块

### 4.1 AI 聊天

**访问路径**: 侧边栏 → Chat（首页）

#### 基本使用

1. 在底部输入框输入问题或指令
2. 按 Enter 或点击发送按钮
3. Agent 会逐步思考、调用工具、检索知识，最后生成回答
4. 回答过程中可以看到 Agent 的**思考过程**（thought）和**工具调用**（tool_call）

#### 会话管理

- **新建会话**：点击侧边栏「+ 新对话」按钮
- **切换会话**：点击侧边栏会话列表中的会话
- **重命名会话**：双击会话标题进行编辑
- **删除会话**：点击会话右侧的删除图标

#### Agent 工作流程

```
用户输入 → 意图分类 → Agent 推理循环（最多10轮）
  ├→ 调用工具（检索/查库/搜索/计算）
  ├→ 分析工具结果
  ├→ 可能再次调用工具
  └→ 生成最终回答
```

#### 支持的提问类型

| 类型 | 示例 |
|---|---|
| 知识库查询 | "上传的合同里关于违约条款是怎么规定的？" |
| 数据库查询 | "查询所有状态为 pending 的订单" |
| 数据修改 | "把用户 Alice 的角色改为 admin"（需审核） |
| 网络搜索 | "最新的 Python 3.13 有什么新特性？" |
| 计算 | "计算 (15000 + 8000) * 1.13" |

---

### 4.2 文档管理

**访问路径**: 侧边栏 → 文档

#### 支持的文件格式

| 格式 | 扩展名 | 说明 |
|---|---|---|
| 纯文本 | `.txt` | 无格式文本 |
| Markdown | `.md` | 支持 Markdown 语法 |
| CSV | `.csv` | 逗号分隔表格数据 |
| PDF | `.pdf` | 含 OCR 回退（扫描件自动识别文字） |
| Word | `.docx` | Microsoft Word 文档 |
| Excel | `.xlsx` | Microsoft Excel 表格 |

**文件大小限制**: 最大 20MB

#### 上传文档

1. 点击「上传文档」按钮
2. 选择一个或多个支持格式的文件
3. 文档自动进入处理流程：

```
已上传 → 解析中 → 分块中 → 向量化 → 索引中 → 就绪
```

4. 处理完成后，文档状态变为「就绪」，即可在聊天中检索

#### 文档列表

- 显示所有文档，支持按状态筛选
- 每行显示：文件名、类型、大小、分块数、状态、上传时间
- 支持操作：
  - **重新处理**：重新解析和索引文档
  - **删除**：删除文档及其所有向量/索引数据

#### 重复检测

系统通过 SHA-256 哈希检测重复文件。上传相同内容的文件会被拒绝。

---

### 4.3 数据库操作

**访问路径**: 侧边栏 → 数据库

数据库面板分为三个标签页：

#### 标签 1: 数据浏览

- **左侧**：数据库结构树，显示所有表名
  - 点击表名查看表结构（字段名、类型、是否可空、键类型）
  - 展开后显示字段列表
- **右侧**：数据预览表格
  - 点击表名加载数据
  - 支持按列排序、分页浏览（每页 50 条）

#### 标签 2: SQL 查询

SQL 编辑器支持两类操作：

**查询操作 (SELECT)**
1. 在编辑器中输入 SQL 语句
2. 系统自动识别为只读查询
3. 点击「执行」直接运行，返回结果（最多 500 行）

**写操作 (INSERT / UPDATE / DELETE)**
1. 输入 SQL 语句
2. 点击「预览」查看：
   - 操作类型和影响行数
   - 受影响的数据预览（前 10 行）
   - 安全等级和警告信息
3. 点击「提交审核」进入审核队列
4. 在审核弹窗中：
   - 查看 SQL 详情和影响数据
   - 填写操作理由
   - 点击「批准」执行，或「拒绝」放弃

#### 安全规则

| 规则 | 触发条件 | 处理方式 |
|---|---|---|
| 无 WHERE 写操作 | DELETE/UPDATE 不含 WHERE | 直接拒绝 |
| 危险操作 | DROP / TRUNCATE / ALTER | 直接拒绝 |
| WHERE 1=1 | WHERE 条件恒真 | 检测并拒绝 |
| SQL 注入 | UNION / SELECT 嵌套 / 注释符号 | 检测并拒绝 |
| 多语句 | 分号分隔的多条 SQL | 拒绝执行 |

#### 标签 3: 操作日志

- 显示所有数据库操作的历史记录
- 支持按操作类型、表名、状态筛选
- 每行显示：操作类型、SQL 预览、影响行数、状态、时间
- 点击行展开查看完整 SQL

#### 审核工作流

```
提交审核 → 等待审核 → 批准/拒绝
                ↓
          批准 → 自动备份 → 执行 SQL → 记录日志
                ↓
          拒绝 → 丢弃操作 → 记录日志
```

#### 回滚操作

如果写操作执行后发现问题：
1. 在操作日志中找到对应的操作记录
2. 使用 backup_id 调用回滚接口
3. 系统自动恢复执行前的数据状态

#### 连接管理

- 首次进入数据库面板，系统自动检测目标数据库连接
- 连接失败时弹出错误详情和建议修复方案
- 可通过「重新连接」按钮手动重试
- 在设置页面修改数据库配置后可重连

---

### 4.4 系统设置

**访问路径**: 侧边栏 → 设置

#### LLM 大模型配置

| 字段 | 说明 |
|---|---|
| Provider | 供应商名称（如 openai、deepseek） |
| Model | 模型名称（如 gpt-4o、deepseek-chat） |
| API Key | API 密钥（保存时自动加密存储） |
| Base URL | API 接口地址 |

点击「测试 LLM 连接」验证配置是否正确。

#### Embedding 嵌入模型配置

同上格式。点击「测试 Embedding 连接」验证。

#### 检索设置

| 参数 | 默认值 | 说明 |
|---|---|---|
| retrieval_top_k | 8 | 检索返回的文档块数量 |
| web_search_max_results | 5 | 网络搜索最大结果数 |

#### 功能开关

| 开关 | 默认 | 说明 |
|---|---|---|
| Web Search | 开 | 允许 Agent 使用网络搜索 |
| Reranker | 开 | 启用 Cross-Encoder 对检索结果精排 |
| Dedup | 开 | 对重复检索结果去重 |
| Memory | 开 | 启用长期记忆功能 |
| OCR | 开 | 对扫描件 PDF 自动 OCR |

> **注意**：功能开关修改后立即生效，无需重启。LLM/Embedding 凭据修改后保存到 `.env` 并热加载。

---

### 4.5 记忆管理

**访问路径**: 侧边栏 → 记忆

#### 记忆列表

- 显示所有长期记忆条目
- 每条记忆显示：内容、类型标签、创建时间、废弃状态
- 支持按类型筛选（fact / preference / decision 等）
- 统计：总计 / 活跃 / 废弃 数量

#### 操作

| 操作 | 说明 |
|---|---|
| 编辑 | 修改记忆内容 |
| 废弃/恢复 | 标记记忆为废弃（不再使用）或恢复 |
| 删除 | 永久删除单条记忆 |
| 清除全部 | 删除所有记忆（需二次确认） |

#### 用户画像

- 点击「生成用户画像」按钮
- 系统分析所有活跃记忆，自动提炼用户摘要
- 画像包含：角色、偏好、决策历史、关键事实等

#### 记忆来源

记忆由系统在以下场景自动提取：
1. 用户对话中明确表达的事实和偏好
2. Agent 执行操作后的重要结果
3. 用户对 Agent 回答的反馈

---

## 5. Agent 工具参考

Agent 可使用 9 个工具完成不同任务。以下是每个工具的详细说明。

### 知识库工具

#### `search_docs` — 搜索知识库

搜索用户上传文档中的相关内容。

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `query` | string | 是 | 自然语言搜索查询 |
| `top_k` | integer | 否 | 返回结果数量（默认使用系统配置） |
| `document_id` | string | 否 | 限定在特定文档内搜索 |

#### `list_documents` — 列出文档

列出知识库中所有文档的摘要信息。

无参数。

#### `get_document_info` — 文档详情

获取某个文档的详细信息。

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `document_id` | string | 是 | 文档 ID |

### 数据库工具

#### `get_db_schema` — 查看表结构

查看目标数据库的表结构。

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `table_name` | string | 否 | 表名。不填则返回所有表的摘要 |

#### `query_db` — 查询数据

执行只读 SELECT 查询。

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `sql` | string | 是 | 完整的 SELECT 语句 |

**限制**：
- 仅允许 SELECT 查询
- 自动添加 LIMIT 500
- 禁止多语句
- 禁止写操作关键字

#### `execute_sql` — 提交写操作

提交 INSERT/UPDATE/DELETE 操作，进入人工审核队列。

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `sql` | string | 是 | 完整的写操作 SQL |
| `reason` | string | 是 | 操作理由说明 |

**限制**：
- DROP/TRUNCATE/ALTER 直接拒绝
- DELETE/UPDATE 无 WHERE 直接拒绝
- 需通过安全规则检查

### 通用工具

#### `web_search` — 网络搜索

搜索互联网获取实时信息。当知识库无法回答时自动使用。

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `query` | string | 是 | 搜索关键词 |
| `max_results` | integer | 否 | 最大结果数（默认 5，上限 10） |

#### `recall_memory` — 记忆召回

搜索用户的长期记忆。

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `query` | string | 是 | 搜索查询 |
| `top_k` | integer | 否 | 返回数量（默认 5） |

#### `calculator` — 计算器

安全地计算数学表达式。仅支持 `+ - * / ()` 运算。

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `expression` | string | 是 | 数学表达式，如 `(100 + 50) * 0.13` |

---

## 6. 安全机制

### 6.1 认证

- 后端所有 API 受 Bearer Token 认证保护
- API Key 首次启动自动生成，写入 `.env` 文件
- 前端请求自动携带认证头

### 6.2 密钥加密

- LLM API Key、Embedding API Key、数据库密码等敏感信息使用 **AES-256-GCM** 加密后存储
- 加密密钥 `SECRET_KEY` 不写入 `.env`，应设为系统环境变量 `SQL_RPA_SECRET_KEY`
- 加密值在 `.env` 中以 `ENC:` 前缀标识

### 6.3 SQL 注入防护

- WHERE 子句使用正则表达式检测注入模式：
  - `UNION` / `SELECT` 关键字
  - SQL 注释 `--` / `/* */`
  - 恒真条件 `OR 1=1`
  - 分号多语句
- 表名使用正则白名单校验 `[a-zA-Z_][a-zA-Z0-9_]*`
- 排序字段使用列名白名单校验

### 6.4 写操作安全

| 防护层 | 机制 |
|---|---|
| 第1层 | SafetyChecker 检查危险关键字 |
| 第2层 | WHERE 子句注入检测 |
| 第3层 | 无 WHERE 写操作拒绝 |
| 第4层 | 影响行数预览 + 数据预览 |
| 第5层 | 人工审核确认 |
| 第6层 | 执行前自动备份 |
| 第7层 | 审计日志记录 |

### 6.5 请求限制

- 请求体最大 10MB（SecurityMiddleware）
- 文件上传最大 20MB
- 查询结果最多 500 行
- Agent 单次最多 10 轮推理，最长 120 秒

---

## 7. 常见问题

### 连接问题

**Q: 数据库面板显示"数据库连接失败"？**
- 检查目标数据库配置是否正确（设置页面可修改）
- SQLite：确认 `db_sqlite_path` 路径存在且可读写
- MySQL：确认主机地址、端口、用户名、密码正确，数据库服务正在运行
- 点击「重新连接」按钮重试

**Q: LLM 连接测试失败？**
- 检查 Base URL 格式是否正确（需包含 `/v1` 后缀）
- 检查 API Key 是否有效
- 检查网络是否可以访问 API 地址
- 如果使用代理，确认代理设置

### 文档处理

**Q: PDF 文档处理后内容为空？**
- 确保 OCR 功能已开启（设置页面）
- PDF 可能是纯图片扫描件，系统会自动触发 OCR
- OCR 需要安装 Tesseract：`apt install tesseract-ocr tesseract-ocr-chi-sim`

**Q: 上传文档提示重复？**
- 系统自动检测内容相同的文件
- 如需更新文档内容，先删除旧文档再上传新版本
- 也可以使用「重新处理」功能

### Agent 行为

**Q: Agent 回答不准确？**
- 确认已上传相关文档到知识库
- 检查检索设置（top_k 值可适当调大）
- 开启 Reranker 可提升检索精度
- 尝试在提问时更具体地描述需求

**Q: Agent 不调用网络搜索？**
- 确认 Web Search 开关已开启
- Agent 优先使用知识库，仅在知识库无答案时触发网络搜索
- 可以明确要求："请在网上搜索..."

**Q: 写操作一直"等待审核"？**
- 进入数据库面板，查看审核队列
- 在操作日志或审核弹窗中批准或拒绝该操作
- 写操作不会自动执行，必须有人工确认

### 性能

**Q: 首次启动较慢？**
- 系统会加载 Reranker 模型（约 1.1GB），首次需要下载
- 国内用户建议设置 `HF_ENDPOINT` 镜像加速（如 `https://hf-mirror.com`）
- OCR 模型也是懒加载，首次使用会有延迟

**Q: 文档处理速度慢？**
- 嵌入向量生成速度取决于 API 供应商
- 大文件（数百页 PDF）处理可能需要几分钟
- 处理进度可在文档列表中实时查看
