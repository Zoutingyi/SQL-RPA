# SQL-RPA 整改前后端分工

最后核查：2026-08-20

本文将 [整改待办清单.md](./整改待办清单.md) 中尚未闭环的事项拆为后端和
前端两条执行线。跨端事项会同时出现在两部分，但分别描述各端交付物，不重复
计算整改项数量。

## 一、后端整改

### A. 交付阻断项（最高优先级）

- [ ] **S-01 SQL 注入与标识符安全**
  - 盘点全部动态 SQL，将条件值、INSERT/UPDATE 值统一改为参数绑定。
  - 表名和列名只允许来自数据库元数据白名单，并按 SQLite/MySQL 方言引用。
  - 为注释、UNION、多语句、转义字符和恶意标识符增加回归测试。
- [ ] **S-03/S-08 密钥管理**
  - 为密文增加 key version，支持新旧密钥并行解密和渐进式重加密。
  - 生产环境接入 KMS/密钥服务，形成轮换、吊销和恢复流程。
- [ ] **S-04/R-03 事务、幂等和失败补偿**
  - 为审批执行引入幂等键和唯一约束。
  - 将目标库写入置于明确事务中；内部审核库采用 outbox/补偿状态记录跨库结果。
  - 覆盖并发批准、执行超时、日志失败、备份失败和进程崩溃恢复测试。
- [ ] **S-06 审计日志防篡改**
  - 提供哈希链全量/增量校验命令和定期校验任务。
  - 通过数据库权限或触发器禁止 UPDATE/DELETE，或同步到外部 append-only 存储。
  - 对断链、重复序号和历史记录修改产生告警。
- [ ] **E-03 MySQL/Qdrant 集成验收**
  - 增加真实 MySQL、Qdrant 容器测试，覆盖连接、Schema、查询、写入、回滚和向量检索。
  - 将集成测试加入 CI；通过前保持“实验性能力”声明。
- [ ] **C-01 多租户、配额和计费（条件阻断）**
  - **联调状态（2026-08-23）**：后端已反馈完成 Tenant/Membership、`tenant_id` 与租户上下文整改；前端已按新契约完成接入。数据隔离和越权防护仍以服务端强制校验为准。
  - 建立 Tenant、Membership、Quota、Usage、Billing 等模型。
  - 所有业务对象增加 `tenant_id` 并在仓储/API层强制隔离。
  - 增加租户级 token/请求配额、成本计算、超额拒绝及审计。

### B. P1 后端完善

- [ ] **H-03 LLM 韧性**：增加熔断器、主备模型、自动降级和降级事件记录。
- [ ] **H-04 用量与成本**：维护模型价格表，记录成本、租户、用户和请求关联，提供聚合API。
- [ ] **R-02 审批策略引擎**：将表、字段敏感度、操作类型、影响行数和审批人数抽象为可版本化策略。
- [ ] **R-04 审批状态机**：实现过期、撤销、升级、转派状态及合法转换校验；产生领域事件供通知使用。
- [ ] **R-05 受控回滚**：反向备份失败时停止回滚；记录原审核、原备份、反向备份和回滚日志关联。
- [ ] **E-01 CI/CD**：补充真实依赖集成作业，并把测试、lint、build配置为必需检查。
- [ ] **E-06 API契约**：统一错误响应结构和错误码，完善认证、权限、分页、限流与示例。
- [ ] **E-07 可观测性**：接入指标、告警和慢查询；记录LLM耗时/错误/成本；生产限流迁移Redis。

### C. P2/P3 后端演进

- [ ] **E-08**：引入数据库迁移工具、语义化版本、CHANGELOG和Release流程。
- [ ] **C-03**：建立并发、容量和延迟基准，输出最低/推荐资源规格。
- [ ] **C-04**：实现PostgreSQL连接器及集成测试。
- [ ] **H-06**：建立黄金SQL、RAG和安全对抗评测集，Prompt按版本存储并进入CI。
- [ ] **C-05**：实现通知事件、站内通知和Webhook/邮件/IM适配器，支持失败重试。
- [ ] **S-09**：快照分块存储、容量上限、租户配额和精细过期清理。
- [ ] **C-06**：OIDC/SAML认证、账号映射和企业身份源配置。
- [ ] **C-07**：审计导出、数据留存策略和PII扫描服务。
- [ ] **E-09**：多实例部署、Redis共享状态、健康检查和优雅关闭。
- [ ] **H-07**：按任务、租户、成本路由模型，并实现自动降级。
- [ ] **S-10**：明确目标库、内部库、备份及传输加密方案并提供部署检查。
- [ ] **R-07**：实现多级审批、会签、转派和工作流版本管理。
- [ ] **C-09**：按目标市场准备等保/ISO所需控制项、证据和审计材料。

### D. 后端当前工程问题

- [ ] 修复 `TestRerankerIntegration.test_retriever_uses_reranker`，使用mock/fake embedding隔离外部凭据。
- [ ] 增加上述安全、事务、RBAC和跨租户边界的自动化测试。

## 二、前端整改

### A. 交付相关功能

- [ ] **C-01 多租户、配额和计费（条件阻断）**
  - **✅ 前端已完成（2026-08-23）**：已完成登录租户上下文、租户切换、成员管理、租户角色/无权限状态、租户用量/配额/账单展示；全部 API、SSE 请求均携带后端校验的 `X-Tenant-ID`，切换租户时会清空租户作用域缓存。前端 lint、15 项测试及生产构建均已通过。
  - 增加租户切换、成员管理、角色展示和无权限状态。
  - 增加用量、配额、成本/账单页面和超额提示。
  - 所有请求携带后端规定的租户上下文，禁止依赖前端自行过滤数据。
- [ ] **R-02 审批策略配置**
  - 提供策略列表、编辑、启停、版本和优先级界面。
  - 清晰展示规则冲突、命中原因和最终所需审批人数。
- [ ] **R-04 审批状态机与通知**
  - **✅ 前端已完成（2026-08-22）**：状态展示、合法操作入口、指派限制提示、通知中心、未读数和审批跳转能力已接入后端契约；后端授权与租户边界仍须独立验收。
  - 展示过期、撤销、升级、转派等状态和允许操作。
  - 增加站内通知入口、未读状态和审批跳转。
- [ ] **R-05 受控回滚**
  - **✅ 前端已完成（2026-08-22）**：回滚确认、风险/影响/原因、完整结果链与失败防误报已实现；后端反向备份原子性仍须独立验收。
  - 二次确认中展示目标备份、预计影响行数、风险和回滚原因。
  - 展示原操作、反向备份和回滚结果的完整链路；失败时禁止误报成功。

### B. 测试与交付质量

- [ ] **E-05 前端自动化测试**
  - Vitest覆盖登录/RBAC、审核状态、双人审批、回滚确认和错误状态。
  - Playwright覆盖登录→查询→提交审核→批准→执行→回滚完整流程。
  - 增加SSE断线、重连、重复事件和流式错误测试。
- [ ] **E-01 CI/CD配合**：保证前端lint、单元测试、E2E和生产构建均可在CI无交互运行。
- [ ] **E-06 API契约配合**：按统一错误码展示用户可读提示，区分认证、权限、冲突、限流和服务异常。
  - **✅ 前端已完成（2026-08-22）**：统一错误分类、结构化错误码与请求 ID 展示已实现。
- [ ] **E-07 可观测性配合**：上报前端异常、页面性能和请求ID，错误页面提供可追踪信息。
  - **✅ 前端已完成（2026-08-22）**：异常、未处理 Promise、页面性能、导航和请求 ID 已接入 `/api/telemetry`。

### C. 产品体验与商业化

- [ ] **H-04 用量与成本**：展示按时间、模型、租户和用户聚合的token及成本，支持配额预警。
- [ ] **C-02 产品化体验**：增加首次配置向导、模型预设说明、空状态、用量解释和账单入口。
  - **✅ 前端已完成（2026-08-22）**：首次配置向导、模型说明、空状态、用量解释和账单入口已实现。
- [ ] **R-06 审批UI增强**：实现before/after差异、风险评分、批量操作和审计时间线。
  - **✅ 前端已完成（2026-08-22）**：已使用后端 `execution_result`、`risk_score`，并接入批量审批和审计时间线。
- [ ] **C-05 通知渠道**：提供通知偏好、Webhook/邮件/IM配置及测试发送界面。
  - **✅ 前端已完成（2026-08-22）**：通知偏好、端点配置、站内通知和测试发送界面已接入真实接口；多 worker 抢占和 SSRF 属后端待验收项。
- [ ] **C-06 SSO**：提供企业登录入口、身份源配置和登录错误引导。
- [ ] **C-07 合规能力**：提供审计导出、留存策略和PII扫描结果界面。
- [ ] **C-08 国际化**：抽取全部界面文案，支持语言切换、日期数字格式和后端错误码本地化。
- [ ] **R-07 审批工作流**：提供多级审批、会签和转派的配置及进度展示。
- [ ] **C-09 合规展示**：按产品要求提供合规状态、审计材料下载或管理入口。

### D. 前端当前工程问题

- [ ] 将 `ConfirmDialog.tsx` 的共享导出移出组件文件，清除Fast Refresh lint警告。
  - **✅ 前端已完成（2026-08-22）**：共享 Context/Hook 已移至独立文件。
- [ ] 调整Tailwind/Vite CSS处理链，清除生产构建中的未知规则警告。
  - **✅ 前端已完成（2026-08-22）**：Tailwind Vite 插件已接入，生产构建无未知规则警告。

## 三、建议排期

### 第一批：恢复可验收状态

- 后端：失败测试、S-01、S-04/R-03、S-06、E-03。
- 前端：E-05核心审核E2E、构建警告清理、回滚失败状态展示。

### 第二批：完善首版交付

- 后端：H-03、R-02、R-04、R-05、E-06、E-07。
- 前端：审批策略、状态机、通知、审批差异与审计链。

### 第三批：商业化与规模化

- 若目标为SaaS，前后端优先共同完成C-01。
- 随后推进SSO、合规、国际化、高可用、多模型路由和复杂审批工作流。
