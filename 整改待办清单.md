# SQL-RPA 整改待办清单

最后核查：2026-08-27

前端、后端的具体职责及建议排期见
[整改前后端分工.md](./整改前后端分工.md)。

状态说明：

- `[x]` **已完成**：代码、必要自动化测试及本地验收证据已具备。
- `[~]` **部分完成**：主体已实现，但仍有明确缺口，或缺少真实环境/远端验收证据。
- `[ ]` **未完成**：尚未发现可用实现，或实现不足以形成主体能力。
- `[-]` **本次不纳入完成度**：按当前决策暂不考核，不计入本轮完成率。
- **部分完成**：已有实现，但未覆盖整改要求的全部范围或缺少验收。
- **未完成**：未发现满足要求的完整实现。

本轮明确不考核 P3 商业化与规模化。其余状态依据当前工作区代码、
2026-08-27 本地回归和现有验收报告重新判定；历史文档中的旧勾选状态不再作为当前结论。

本轮验证证据：后端 `227 passed, 5 skipped`；前端 `48 passed`；lint、组织契约、
`user-api-v1` 契约和生产构建通过；Playwright `15 passed`。5 个后端跳过项均为真实
MySQL、PostgreSQL、Qdrant、Redis/多进程集成场景。

## 一、核心安全与交付阻断项

- [~] **S-01（部分完成）SQL 注入防护与标识符校验**
  - 已完成单语句限制、危险语句检查、表名/列名白名单、方言化标识符引用及主要查询参数绑定，相关安全测试通过。
  - 尚缺对全部动态 SQL 拼接点的最终清零审计，真实 MySQL 场景仍需随 E-03 验收。
- [x] **S-03（已完成）密钥管理加固（非 KMS 范围）**
  - 已实现版本化密钥、兼容旧密文、凭据和备份批量重加密、轮换接口及回归测试。
  - 外部 KMS 属于本轮不考核的商业化增强，不影响本项功能完成判定。
- [x] **S-04（已完成）备份与回滚事务一致性**
  - 已实现加密分块快照、容量与保留限制、目标库写入/内部状态的 Saga 补偿、失败恢复与重试测试。
- [x] **S-06（已完成）审计日志防篡改**
  - 已实现序号、哈希链、完整性校验、恢复任务和 SQLite append-only 触发器，并有自动化测试。
- [x] **R-03（已完成）审批执行事务化、幂等与并发锁**
  - 已实现组织级幂等键、状态条件更新、并发审批保护、四眼审批、执行补偿和防重复执行测试。
- [~] **E-03（部分完成）MySQL/Qdrant 集成测试与能力声明**
  - 连接器、真实服务测试和 CI 服务编排已完成。
  - 当前本地回归因未启动真实服务而跳过；取得远端 CI 成功记录前继续标记部分完成。
- [~] **C-01（部分完成）多租户/四级组织隔离、配额与计费**
  - 四级组织、主职/兼职、上下文令牌、纵向资源范围、连接隔离、配额和账单主体已完成；租户 Bugbot 清单 9 项代码整改均已记录完成，部门 E2E 通过。
  - 尚待当前修复版本重新执行 Bugbot/Security Review，并取得 PostgreSQL、Redis真实集成门禁结果。

## 二、P1 功能完善

- [x] **H-03（已完成）LLM 客户端韧性**：已实现超时、重试、熔断、半开恢复、主备模型降级及降级事件测试。
- [x] **H-04（已完成）LLM 用量与成本计量**：已实现 token/模型/成本记录、组织聚合、原子配额预留结算、用量页和账单能力。
- [~] **R-02（部分完成）审批策略引擎**：后端已支持表、字段、操作类型、影响行数和审批人数策略及 API；仍缺完整的策略可视化管理页面。
- [x] **R-04（已完成）审批状态机与通知**：已实现过期、撤销、升级、转派、指派、站内通知和 Webhook 投递闭环，前后端操作入口及测试已具备。
- [x] **R-05（已完成）回滚操作受控**：反向备份失败会停止回滚，已具备状态校验、失败补偿、恢复任务及回归测试。
- [~] **E-01（部分完成）CI/CD 自动化验收**：CI 已包含后端、前端、契约、Playwright、迁移和真实服务聚合门禁；当前 `.github` 及相关代码尚未提交远端，主分支 required check 也无本轮执行证据。
- [~] **E-05（部分完成）前端自动化测试**：Vitest 48 项和 Playwright 15 项通过，已覆盖用户、组织切换、迟到 HTTP/SSE、审核组件等；仍缺连接真实后端的审核执行与回滚浏览器 E2E。
- [~] **E-06（部分完成）API 文档与错误码契约**：FastAPI OpenAPI、统一错误响应、request ID、用户 V1 快照和 README API 模块说明已完成；尚缺覆盖全部接口的独立 API reference、分页/限流及错误码目录。
- [~] **E-07（部分完成）可观测性**：已实现请求 ID、慢请求与接口指标、管理员 metrics、LLM 成本/降级事件和 Redis共享限流；外部指标采集、告警路由和运行值班验收尚未完成。

## 三、P2 近期迭代

- [~] **E-08（部分完成）版本与迁移流程**：已具备语义化版本、CHANGELOG、Alembic、迁移前后校验和升级/回滚脚本；尚未形成已发布 Release 和远端流水线证据。
- [x] **C-02（已完成）Onboarding、用量解释和账单页面**：前端页面、路由、接口及基础测试已完成。
- [~] **C-03（部分完成）并发、容量、延迟基准及资源规格**：已有基准脚本、指标门槛和首版资源规格文档；尚缺在固定硬件与代表性数据集上的实测报告。
- [~] **C-04（部分完成）PostgreSQL 连接器与集成测试**：连接器、配置和真实集成测试代码已完成；当前环境未执行真实 PostgreSQL 作业。
- [x] **R-06（已完成）差异、风险评分、批量审批和审计视图**：before/after、风险因素、批量批准/拒绝及前端审核队列已经实现并通过测试。
- [x] **H-06（已完成）评测集及 Prompt 版本化**：黄金 SQL、安全对抗、RAG 数据集、评测门禁和 Prompt 内容哈希版本记录已完成。
- [~] **S-08（部分完成）密钥轮换和 KMS 接入**：应用内版本化轮换、旧密文迁移和测试已完成；外部 KMS 接入属于本轮不考核的商业化增强。
- [x] **C-05（已完成）站内及 Webhook 通知**：站内通知、偏好、端点 CRUD、原子投递、SSRF 防护和审核事件通知已完成；邮件/IM 为可选后续渠道。
- [x] **S-09（已完成）快照分块、容量和保留策略**：加密压缩、分块、单次/总量上限、有效期、清理和失败测试均已实现。

## 四、P3 商业化与规模化（本次不纳入完成度）

- [-] **C-06** SSO/OIDC/SAML。
- [-] **C-07** 合规导出、数据留存策略和 PII 扫描。
- [-] **E-09** 高可用、水平扩展和 Redis共享限流。
- [-] **H-07** 多模型路由和自动降级。
- [-] **S-10** 目标数据库、内部库、备份和传输加密评估。
- [-] **C-08** 前后端及系统提示词国际化。
- [-] **R-07** 多级审批、会签和转派工作流。
- [-] **C-09** 等保、ISO 或目标市场所需安全认证。

## 五、当前测试与构建问题

- [x] **已完成**：Reranker 测试已隔离外部凭据；当前后端完整回归通过。
- [x] **已完成**：前端 lint 通过，原 ConfirmDialog Fast Refresh 问题已清理。
- [x] **已完成**：前端生产构建通过，当前未出现旧 Tailwind/Lightning CSS 未知规则告警。
- [~] **部分完成**：5 个真实服务集成场景在本机被跳过，等待远端 CI 的 PostgreSQL、Redis、MySQL和Qdrant执行结果。

## 六、当前剩余工作顺序（不含商业化）

1. 对剩余动态 SQL 路径完成最终审计，关闭 S-01。
2. 整理并提交当前 180 项本地变更，运行远端完整 CI，取得真实 PostgreSQL、Redis、MySQL和Qdrant验收结果。
3. 重新执行租户及用户整改 Bugbot 和 Security Review，确认当前修复版本无 P1。
4. 补齐总体 API reference、错误码目录和真实后端审核/回滚浏览器 E2E。
5. 在固定资源和代表性数据集上执行容量测试，归档 P50/P95/P99、吞吐、资源和错误率报告。
6. 根据上述证据把 E-03、C-01、E-01、E-05、E-06、E-07、E-08、C-03、C-04 从部分完成更新为已完成。
